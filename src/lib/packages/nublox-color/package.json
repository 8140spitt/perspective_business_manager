{
  "name": "@nublox/color",
  "version": "0.2.0",
  "description": "Explainable perceptual colour definition engine for ramps, palettes, themes, accessibility and design tokens.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && node dist/demo.js",
    "test": "npm run build && node dist/__tests__/basic.test.js"
  },
  "keywords": ["oklab", "oklch", "color", "colour", "design-system", "accessibility", "wcag", "tokens"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
